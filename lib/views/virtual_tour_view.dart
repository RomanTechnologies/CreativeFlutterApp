import 'package:flutter/flutter.dart';
import 'package:panoramic/viewer.panoramic_viewer.dart';

class VirtualTourView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return PanoramicViewer(
      // Initial settings and configuration for virtual tours.
    );
  }
}
