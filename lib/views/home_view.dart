import 'package:flutter/flutter.dart';

class HomeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(\n      child: Center(\n        child: Text('Welcome to Virtual Travel Guide'),\n      ),\n    );
  }
}
