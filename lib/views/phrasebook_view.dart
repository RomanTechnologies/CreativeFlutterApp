import 'package:flutter/flutter.dart';
import '../widgets/phrasebook_item.dart';

class PhrasebookView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scrollable(
      child: Column(
        children: [\n          // This screen will display a list of phrasebook items.\n        ],\n      ),\n    );
  }
}
